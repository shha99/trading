# 시그널 재계산 주기(분)
REFRESH_INTERVAL_MINUTES=15

# 서버 기동 시 스케줄러 자동 시작 여부 (테스트 시 false 권장)
ENABLE_SCHEDULER=true

# 프론트엔드 도메인만 허용하려면 콤마로 구분해서 지정 (기본값 *)
CORS_ORIGINS=*

# SQLite 기본 사용. PostgreSQL 등으로 바꾸려면 예: postgresql://user:pass@host/db
# DATABASE_URL=sqlite:////app/data/signals.db
